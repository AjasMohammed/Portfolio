# Packages whose install scripts are allowed to run. workerd is the one that
# matters: its postinstall downloads the Workers runtime binary, without which
# `wrangler dev`/`deploy` can't start.
allowBuilds:
  esbuild: true
  sharp: false
  unrs-resolver: true
  workerd: true
